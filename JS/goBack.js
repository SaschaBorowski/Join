/**
 * Navigates the browser to the previous page in the history stack.
 * This function uses the `history.back()` method to go back one step in the browser's history,
 * effectively mimicking the behavior of the browser's back button.
 *
 * @function
 */
function goBack() {
    history.back();
}